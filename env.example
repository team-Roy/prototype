# ============================================
# Frontend 환경변수 (apps/web/.env.local)
# ============================================

# API URL
NEXT_PUBLIC_API_URL=http://localhost:3001

# Public URLs
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_STORAGE_URL=http://localhost:9000/fandom-media

# Analytics (optional)
NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_SENTRY_DSN=


# ============================================
# Backend 환경변수 (apps/api/.env)
# ============================================

# App
NODE_ENV=development
PORT=3001
APP_URL=http://localhost:3000
API_URL=http://localhost:3001

# Database (PostgreSQL)
# 로컬: docker-compose 사용 시
DATABASE_URL=postgresql://fandom:fandom_local_dev@localhost:5432/fandom_lounge
# 프로덕션: Neon 사용 시
# DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/fandom_lounge?sslmode=require

# Redis
# 로컬: docker-compose 사용 시
REDIS_URL=redis://localhost:6379
# 프로덕션: Upstash 사용 시
# REDIS_URL=rediss://default:xxx@xxx.upstash.io:6379

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production
JWT_REFRESH_EXPIRES_IN=7d

# Bcrypt
BCRYPT_ROUNDS=12

# Storage (S3 Compatible - Cloudflare R2)
# 로컬: MinIO 사용 시
STORAGE_ENDPOINT=http://localhost:9000
STORAGE_ACCESS_KEY=minioadmin
STORAGE_SECRET_KEY=minioadmin123
STORAGE_BUCKET=fandom-media
STORAGE_REGION=auto
STORAGE_PUBLIC_URL=http://localhost:9000/fandom-media

# 프로덕션: Cloudflare R2 사용 시
# STORAGE_ENDPOINT=https://xxx.r2.cloudflarestorage.com
# STORAGE_ACCESS_KEY=xxx
# STORAGE_SECRET_KEY=xxx
# STORAGE_BUCKET=fandom-media
# STORAGE_REGION=auto
# STORAGE_PUBLIC_URL=https://pub-xxx.r2.dev

# OAuth - Kakao
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=
KAKAO_CALLBACK_URL=http://localhost:3001/auth/kakao/callback

# OAuth - Google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3001/auth/google/callback

# Rate Limiting
RATE_LIMIT_TTL=60000
RATE_LIMIT_MAX=100

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Logging
LOG_LEVEL=debug

# Sentry (optional)
SENTRY_DSN=


# ============================================
# 프로덕션 체크리스트
# ============================================
# 
# [ ] JWT_SECRET, JWT_REFRESH_SECRET을 강력한 랜덤 값으로 변경
# [ ] DATABASE_URL을 Neon 연결 문자열로 변경
# [ ] REDIS_URL을 Upstash 연결 문자열로 변경
# [ ] STORAGE_* 값들을 Cloudflare R2 값으로 변경
# [ ] KAKAO_*, GOOGLE_* OAuth 설정 완료
# [ ] CORS_ORIGINS에 실제 도메인 추가
# [ ] NODE_ENV=production으로 변경
# [ ] SENTRY_DSN 설정 (에러 모니터링)
